# Configuration for deploy/staging

# === SSH Deploy Configuration ===
# SSH host for connecting to server (user@hostname)
SSH_HOST=user@your-server.com

# Project directory on server
PROJECT_DIR=/home/user/telegram-weather-bot-pub

# Branch for deployment
BRANCH=main

# === Bot Configuration ===
# Bot token for deployment testing
BOT_TOKEN=your_deploy_test_bot_token_here

# Bot admin IDs (comma-separated)
ADMIN_IDS=123456789

# Language for admin commands (ru, en, de)
ADMIN_LANGUAGE=ru

# === Spam protection settings for staging (moderate limits) ===
# Uncomment and configure only if you need to change default values

# Maximum requests per minute (default: 30)
SPAM_MAX_REQUESTS_PER_MINUTE=20

# Maximum requests per hour (default: 200)  
SPAM_MAX_REQUESTS_PER_HOUR=100

# Maximum requests per day (default: 300)
SPAM_MAX_REQUESTS_PER_DAY=500

# Block duration when limits exceeded in seconds (default: 300 = 5 minutes)
SPAM_BLOCK_DURATION=300

# Extended block duration for repeat violations in seconds (default: 3600 = 1 hour)
SPAM_EXTENDED_BLOCK_DURATION=1800

# Minimum pause between requests in seconds (default: 1.0)
SPAM_MIN_COOLDOWN=1.0

# Maximum message length (default: 1000)
SPAM_MAX_MESSAGE_LENGTH=1500

# === Weather API quota settings ===
# Maximum weather API requests per rolling day (default: 1000)
WEATHER_API_DAILY_LIMIT=1000
# Path for persisted quota timestamps (default: data/weather_api_quota.json)
WEATHER_API_QUOTA_PATH=data/weather_api_quota.json
